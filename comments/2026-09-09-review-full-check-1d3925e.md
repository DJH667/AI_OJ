# 9.9 评审意见 —— 全量对标检查（需求文档 vs 前后端实现）

- **评审日期**：2026-09-09
- **对应 git**：`1d3925e`（HEAD）
- **对标基准**：`reports/PROJECT_REQUIREMENTS.md`（含全部已确认决策 §9）+ 官方 api.md 转述
- **方法**：逐节比对后端路由（38 个 `@router` 全部核对）与 `frontend/app.py`（1860 行通读）
- **结论**：✅ 基础六步（Step1–6）后端全部实现、前端三组页面齐全；AI R1–R4 主体完成。**有 2 个验收演示级缺口（P1/P2）和若干 P3 待办**，详见 §3。

## 1. 已完成且前端已展示（简表）

| 模块 | 后端 | 前端 |
|---|---|---|
| Step1 题目 | 5 接口 ✅（409/400/404、默认字段、级联删除+统计回退） | 题库卡片（难度/标签/通过率）、详情、新增/编辑完整表单（11 字段）、删除 |
| Step2 评测 | 提交+429 单人单题、语言注册/列表、AC/WA/TLE/MLE/RE/CE/UNK、资源限制 | 提交面板（语言下拉+代码框）、语言管理页（注册表单带字段说明） |
| Step3 评测管理 | 列表（一级条件/分页/摘要裁剪/scope=all）、详情、rejudge | 查询提交记录页（题目/用户范围/状态筛选/分页/详情展开）、管理员重评按钮 |
| Step4 用户 | 注册/登录/登出/详情/列表/role/创建管理员 ✅ | 登录注册页（校验同后端）、个人中心（信息+统计）、用户管理（role 修改，仅管理员） |
| Step5 日志 | log 三态（Q7 语义）、log_visibility、access 审计 ✅ | 提交详情内嵌测试点日志表格 + 403/不可见提示 |
| Step6 前端 | — | 三组页面 ✅、Cookie 登录态持久化 ✅、轮询/自动刷新 ✅、纯 REST 交互 ✅ |
| AI R1–R4 | 任务状态机、per-user model-config、模型目录/汇率自动化、流式进度回调、cancel 真终止、usage/cost 分列 | 两输入界面（结构化/纯文本+上传提示）、模型配置页（目录下拉+计价依据+汇率）、任务进度轮询+终止、Token/费用展示、采纳预填新增表单 |
| 其它 | reset（admin）、通知、申请审批、auth/me、示例题种入 | 信息中心/未读数、申请审批（管理员）、删除/修改申请（普通用户） |

## 2. 对照红线抽查（通过）

- 全部 API `async def` ✅；响应 `{code,msg,data}` + HTTP 状态码一致 ✅；异常顺序与 422→400 ✅
- Session/Cookie（uuid4、服务端存储、7 天、banned 立即失效）✅；bcrypt、api_key 脱敏、error_info 无路径泄露 ✅
- 全 JSON 存储、reset 语义（admin 鉴权+开关）✅；前端不直连数据、不硬编码身份 ✅

## 3. 缺口与未在前端显示项（按优先级）

| # | 级别 | 需求出处 | 现状 | 建议 |
|---|---|---|---|---|
| 1 | **P1** | Step5 `PUT /api/problems/{id}/log_visibility`（日志/测例权限管理，内部权重 2 分） | 后端已实现，**前端无任何入口**——管理员无法从页面设置 `public_cases` | 在题目管理卡片或详情页加管理员"日志公开"开关；9.10 演示前完成 |
| 2 | **P2** | Step5 `GET /api/logs/access/` 审计查询（审计与安全说明 1 分） | 后端已实现，**前端无查看页**（也无"访问被拒 403"的审计可视化） | 个人中心/管理员加"访问审计"页（按 user/problem 筛选+分页） |
| 3 | **P2** | §6.1 站内参考题机制（dec-7b47335beaa64320）：解析纯文本里的站内题目链接；按 `difficulty_score` 邻域（±1）自动抽取相近题 | **未实现**：纯文本中的链接不解析；仅结构化表单下拉可手动选**一道**参考题，无邻域抽取 | 若冲 AI 10 分：pipeline 补链接解析 + 邻域抽题（默认 ±1）；若时间紧按"基础优先"放弃并在文档标注 |
| 4 | P2 | 助教澄清：**普通用户 PUT 可直接改题**（api.md 语义） | 后端 PUT 仍开放 ✅，但**前端普通用户编辑一律走"申请-审批"**，无直接编辑入口 | 与验收口径确认：api.md 走查演示时应能"普通用户直接改题"；建议前端保留直接编辑（或演示时说明两种路径） |
| 5 | P3 | Step4 `POST /api/users/admin` 创建管理员 | 后端 ✅，前端用户管理**无创建管理员入口**（只有 role 修改） | 加"创建管理员"按钮（管理员可见） |
| 6 | P3 | §6.2 用尽重试 → `completed + needs_review` | 实现/前端统一用 `review=True / review_note`（语义一致，命名不同） | 需求文档与代码二选一统一命名，避免报告/答辩口径不一致 |
| 7 | P3 | R4 说明统计/估算方式及限制 | mock 模式下任务页费用/Tokens 为**模拟 usage**，页面未标注"模拟数据" | 前端任务页在 `mock=True` 时加一行说明（接口未回传 mock 标志则前端判断 api_key 未配置） |
| 8 | P3 | §1.1 基础接口不新增变体/自建路径 | `applications`、`notifications`、`scope=all` 为 api.md 外扩展（api.md 契约未破坏） | 在 PROJECT_REQUIREMENTS §9 补一条"polish 扩展接口清单及说明"，满足"等价替换须文档说明" |
| 9 | P3 | 观察项 | 启动自动种入 P1000/P1001（reset 不回种）；自动评测若用同 id 建题会 409 | 验收前用真实评测脚本复核；文档已说明 `OJ_SEED_DEMO=0` |
| 10 | P3 | 文案 | 题目管理页 caption"删除仅管理员可用"与实际（普通用户可提交删除申请）不一致 | 改为"管理员直接删除；普通用户提交申请" |

## 4. 结论与放行

- **后端覆盖**：需求 §5 全部基础接口 + AI 接口均已实现；75/75 测试绿（此前验证）。
- **前端覆盖**：Step6 三组页面齐全、AI 页面齐全；**唯一必改项是 #1（log_visibility 前端入口）**，建议连同 #2（审计页）在 9.9 当天补上；#3/#4 需用户按验收口径拍板。
- 其余 P3 不阻塞验收，可随报告撰写一并处理。
