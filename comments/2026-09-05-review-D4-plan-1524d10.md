# 9.5 评审意见 —— 新计划（WORK_PLAN v3）中 D4 内容

- **评审日期**：2026-09-05
- **对应 git**：`1524d10`（D4 实现；计划 v3 为 `1ec63bd`）
- **评审对象**：`reports/WORK_PLAN.md` v3 中 **D4 9.5（六）晚** 一节，及其与当前实现（`1524d10`）的一致性
- **评审基线**：以当前工作目录文件为准，git 仅作参考
- **结论**：✅ 计划中 D4 内容与官方 Step2 收尾 + Step3 需求对齐，且与实际实现一致、可验收；有 3 处计划表述/排期建议 + 1 个测试稳定性风险需关注。

---

## 1. 先了解计划（要点复述）

- v3 总盘：D1–D3 已完成；剩余 D4（9.5 晚）→ D5（9.6 Step5+AI 对齐）→ D6a（9.7 Step6 前端）→ D7a（9.8 回归）→ AI 冲刺+报告（9.9）→ 验收（9.10）。基础 30 分预计 9.8 前收尾。
- **D4 内容**：Step2 收尾（psutil 内存监控→MLE、details 真实 memory/time、WSL 超内存样例）+ Step3 评测管理（列表/详情/rejudge）。

## 2. 对 D4 计划内容的逐项评审

| 计划条目 | 评审 |
|---|---|
| psutil 内存监控线程（超限 kill → MLE） | ✅ 已实现 `_monitor_memory`（20ms 轮询 RSS、超限 kill、峰值记录），与 FAQ 参考一致 |
| details 记录真实 `memory`(MB) 与 `time` | ✅ `_run_case` 记录峰值与耗时；MLE 测试验证 `memory > 0` |
| WSL 下构造超内存样例验证 | ✅ `test_memory_limit_mle`（16MB 限制 + `bytearray(64MB)` → MLE） |
| `GET /api/submissions/` 列表（一级条件至少其一、二级筛选、分页边界） | ✅ 全空 400、status 过滤、分页复用 `pagination.normalize_page`；测试覆盖全空/page 有 size 无/size 有 page 无/越界 |
| 可见性 本人/管理员 | ✅ 普通用户强制只看自己；`user_id=他人` 403；未登录 401 |
| pending/error 摘要只回 id+status | ✅ `summary()` 裁剪正确（其余含 score/counts） |
| `GET /api/submissions/{id}` 详情（pending 至少 id+status） | ✅ 本人/管理员，他人 403、不存在 404；pending 未产生字段为 null |
| `PUT /api/submissions/{id}/rejudge`（仅管理员、覆盖回 pending、复用 judge 串行锁） | ✅ `reset_for_rejudge` 覆盖为 pending 后经 `_judge_serial` 自动重跑；403/404 已测 |
| 交付：Step3 三接口自测全绿；MLE 样例记录（报告素材） | 🟡 测试全绿（46/46，见 §4）；MLE 目前只有 pytest 用例，无独立截图/素材文件（建议 D5–D7a 按"每日截图存档"补齐） |

## 3. 计划表述/排期建议（针对 D4 一节）

1. **【P3】时间账"等效原计划"列不准确**：D4 行写"= 原 D4 + D5 前半"，但 v3 的 D4 内容（Step2 收尾 + Step3）就是 v2 的 D4 本身，并未吞并 D5 前半；D5 行"= 原 D6 前半"也应为"原 D5 + AI 对齐"。建议修正该列，避免后续排期误读。
2. **【P2】rejudge 的统计口径未写进 D4 计划**：实现与 d4 notes 已定"rejudge 不新计 submit_count、resolve_count 只增不减（重评把唯一 AC 变失败不回溯）"，但这是官方未定义语义，D4 计划交付标准里应补一句"rejudge 统计口径落档并列入待助教确认"，防止 9.8 回归时被当成 bug。
3. **【P2】Q5（CE 状态归属）排期偏晚**：Step3 摘要裁剪对"CE 是 success 还是 error"敏感，但计划只在 9.8 D7a 安排"Q5 回填"。建议提前到 9.6 拿答复；否则若助教判 error，9.8 还要回头改 judge 状态映射 + 列表裁剪，压缩回归时间。
4. **【P3】"MLE 样例记录（报告素材）"落地形式未定义**：建议明确为"截图 + 一条 submission JSON 摘录"存入 `reports/` 或素材目录，报告 §成果展示 可直接引用。

## 4. 实测验证

| 验证项 | 方式 | 结果 |
|---|---|---|
| 全量测试（第 2 轮） | `wsl ~/oj-venv/bin/python -m pytest tests -q` | **46 passed**（2 条依赖弃用警告） |
| 单测复核 | `pytest tests/test_d3_judge.py::test_compile_error_cpp -q` | passed |
| 工作区状态 | `git status --short` | clean |

> **【P2 测试稳定性】第 1 轮全量测试出现 1 次 flaky**：`test_compile_error_cpp` 报 `FileNotFoundError`；单独运行通过，第 2 轮全量 46 passed。初步判断与后台评测任务/全局锁跨用例的时序竞态有关（个别任务在下一用例 reset 后仍在写文件）。D5 前建议定位：在测试夹具 teardown 等待后台评测任务结束，或对 judge 后台任务做显式收敛。

## 5. 结论与放行

- **计划中 D4 内容可直接作为验收依据**：范围完整、语义与官方 Step2/Step3 一致，且 `1524d10` 实现已兑现计划（46/46 测试 + MLE 真实样例）。
- D5（9.6 Step5 + AI 对齐）开工前建议完成三件事：① 修正时间账"等效原计划"列；② 把 rejudge 统计口径写入计划/需求文档并列入待确认；③ 提前追 Q5 答复 + 定位 flaky 测试。
