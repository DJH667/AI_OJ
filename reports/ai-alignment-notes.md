# AI 出题模块 —— 对齐结论与实施定案（2026-09-05 定稿）

> 对齐完成：2026-09-05 经多轮问答（dec-0ba6f434fbc5d4e1 等）全部定案，AI 模块恢复排期（9.8 晚起 + 9.9 全天冲刺）。
> 界面/字段框架见 PROJECT_REQUIREMENTS.md §6.1；实施计划（5 Phase）见会话批准稿。

## 1. 对齐结论（8 项议题 → 定案）

| # | 议题 | 定案 |
|---|---|---|
| 1 | 输入界面范围 | **两种界面都做**：结构化表单（考点多选+其他 / 难度数值档 / 预期复杂度 / 数据规模 / 情景背景 / 备注）+ 纯文本（自然语言）；两者均支持引用站内题目链接；上传文件支持（文本类 ≤10MB，纯文本模型提示"无法观看图片"） |
| 2 | 站内参考题机制 | **做**：解析站内题目链接 → 取题面/配置为上下文；`difficulty_score` 邻域（默认 ±1）抽相近题；参考题仅作模型上下文 |
| 3 | 外部模型接入 | OpenRouter（OpenAI 兼容 `/api/v1`）；**模型由用户自选**（仅支持 OpenRouter 有明确计价的模型）；api_key 可为用户自带的模型方 key 或 **OpenRouter 分发 key**；计价按 OpenRouter 公布的每 1M token 单价录入 `model-config`（input_price/output_price/price_unit），可选从 `/v1/models` 自动拉价；无 key 用本地 mock |
| 4 | Token/费用统计（R4） | 按任务记录 input/output tokens；费用 = 输入token/单位×输入单价 + 输出token/单位×输出单价；页面展示并注明计价依据；mock 也走同一 usage/cost 结构 |
| 5 | 任务模型与进度（R3） | 状态机：等待/执行/完成/中断/失败（+ needs_review）；持久化 `data/ai_tasks/`；进度传输用**轮询**（GET 任务）；cancel **真实终止**（完成态 409） |
| 6 | 产出衔接（R1） | 题目 JSON → 前端跳"新增/编辑"表单**预填**（可人工增删改）；私有键 `difficulty_score` + `ai_meta`（语言/生成器/标答/暴力代码/重试次数）**仅服务端存储、不进任何对外 API**；支持基于已有题目（problem_id）的修改迭代 |
| 7 | 验收演示 | 9.10 演练含 AI 分支（真实 key / mock 两套；普通 / 硬核两模式）；核心演示=硬核生成需 O(N log N) 题 → O(N log N) AC vs O(N²) 部分分梯度 |
| 8 | 优先级与裁量 | 范围"全都要"；若 9.8–9.9 时间吃紧 → 收缩为 R1–R4 + 硬核单轮对拍（保底 ~4–5 分），基础 30 分与报告**不挤占** |

## 2. 追加定案（2026-09-05 问答细化）

1. **语言必选**：命题结构化输入含必选"语言"字段，选项 = **全部已注册语言**（动态取 `GET /api/languages/`，默认 python，含动态注册如 go）。
2. **纯文本语言策略（双层）**：prompt 要求模型遇"未安装/未注册语言"时直接产出"语言不支持"错误（不生成题）；后端解析兜底校验（language ∉ 已注册 → 任务失败，返回清晰原因 + 可用列表）。
3. **硬核模式（可选对拍，默认关）**：前端开关"硬核模式"；开启后显示"对拍重试次数"调节（默认 2）。开启时执行三代码对拍闭环；关闭时普通生成（AI 直接给全量测例，质量靠 prompt + 人工复核）。
4. **对拍引擎**（仅硬核；三代码用所选语言）：数据生成器 → 多档规模数据（小/中/大，规模按期望复杂度推导，使 O(N²) 类在中小点可过、大点 TLE → 部分分梯度）→ 标答算 expected → 小规模点用暴力对照验证 → 通过采纳；不一致/异常 → 错误摘要传回 AI 重试（≤retry_limit）→ 用尽：任务 completed + **needs_review** + 错误摘要（题目不入库）。
   - 引擎兼容**所有已注册语言**：自 judge 抽取语言无关编译/运行原语（services/runner.py）共用；本地执行限时/限内存。
5. **落库内容**：题目采纳 = 题目字段 + `samples`（展示小样例）+ **`testcases` 全量（对拍通过/普通生成的完整测点，含大数据文本）** + 私有 `difficulty_score`/`ai_meta`；needs_review 题不落库。重试反馈只含错误摘要（不塞失败样例）。

## 3. 恢复后的排期（WORK_PLAN 同步）

- Phase 1 文档定案（9.5）；Phase 2 AI 后端（9.8 晚起）；Phase 3 AI 前端（9.8 晚–9.9）；Phase 4 联调演示（9.9）；Phase 5 验收兜底（9.10）。
- 基础（Step6 前端）优先于 AI 前端联调窗口。

---
**对齐结论回填区**：✅ 已完成（本文件即定案）。
**排期更新**：✅ 已同步 WORK_PLAN（AI 从挂起恢复为冲刺排程）。
