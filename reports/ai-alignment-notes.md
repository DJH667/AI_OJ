# AI 出题模块 —— 待对齐事项（占位草稿，2026-09-03）

> 用户指示（2026-09-03）：**AI 模块先不做**，启动前需与用户对齐需要实现的事项，对齐后回填本文件并恢复排期（WORK_PLAN §4）。
> 本节仅列**议题框架**（对齐时逐项讨论定案），不含实现细节。界面/字段框架已定部分见 PROJECT_REQUIREMENTS.md §6.1。

## 对齐议题清单

1. **输入界面范围**：结构化表单（考点多选/难度数值/预期复杂度/数据规模/情景背景/备注）与纯文本两种是否都做？各自字段与"其他"自定义项确认；上传文件支持的类型/大小/纯文本模型提示。
2. **站内参考题机制**：站内题目链接解析、按 `difficulty_score` 邻域抽相近题作为模型上下文——范围确认（是否本期必做）。
3. **外部模型接入**：提供商 OpenRouter（OpenAI 兼容 `/api/v1`）；选择哪个/哪些模型；`model-config`（provider_url/model/api_key/input_price/output_price/price_unit）的默认值与计价来源；API key 由用户提供；无 key 时本地 mock 的保底流程。
4. **Token 用量与费用统计口径（R4）**：按任务记录 input/output token；费用公式与计价单位展示；接口拿不到完整 usage 时的估算说明——确认展示粒度与"计价依据"文案。
5. **任务模型与进度/中断（R3）**：任务状态机（等待/执行/完成/中断/失败）；进度传输方式（轮询 vs SSE，考虑 Streamlit 前端消费）；中断需真实终止任务——确认实现范围。
6. **产出与题目系统衔接（R1）**：AI 产出 → 跳转题目"新增/编辑"表单预填的衔接；`difficulty_score` 等私有字段如何由 AI 侧写入（仅服务端文件、不进 API，见 §9 决策 9）；是否支持"基于已有题目的修改迭代"。
7. **验收演示设计**：9.10 演示 AI 命题的流程与边界用例（合理题/用例有效性/易用性如何呈现）。
8. **优先级与裁量**：若时间紧，哪些功能可裁剪（如仅结构化界面、仅单模型、不做上传文件）。

---
**对齐结论回填区**：（待填写）

**恢复后的排期更新**：（待填写，参考 WORK_PLAN §4）
